package com.geek.text;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication
public class GeekTextServiceApplication {

	// Entry point for the application
	public static void main(String[] args) {
		// The first argument is the main class of the application and the second argument is command-line arguments
		SpringApplication.run(GeekTextServiceApplication.class, args);
	}

}
